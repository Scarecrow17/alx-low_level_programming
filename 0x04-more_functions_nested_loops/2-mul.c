#include <stdio.h>
#include "main.h"


/**
* mul - multiply two integers
* @p: int to multiply to x
* @x: int to multiply to p
* Return: product of x & p
*/
int mul(int p, int x)
{
	return (p * x);
}

